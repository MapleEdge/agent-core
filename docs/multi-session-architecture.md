# Multi Session Architecture

Single VM

Session -> Worktree -> Executor -> Timeline

Many sessions may run concurrently.

agent-core is shared.

jubilant-goggles manages placement, scheduling, authorization and execution.
